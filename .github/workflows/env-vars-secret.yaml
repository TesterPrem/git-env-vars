name: Using Environment variables with Secrets
on: push
jobs:
  Check-the-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: All Secrets
        run: |
          echo "WF_ENV: ${secrets.WF_ENV}"
          echo "JOB_ENV: ${secrets.JOB_ENV}"
          echo "STEP_ENV: ${secrets.STEP_ENV}"
